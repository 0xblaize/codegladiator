import Link from 'next/link'
import { ArrowRight, Building2, ShieldCheck, Sparkles } from 'lucide-react'

export default function SponsorPage() {
  return <main className="arena-grid min-h-screen px-5 py-6 sm:px-8"><header className="mx-auto max-w-5xl"><Link href="/" className="text-sm text-white/55 hover:text-white">← CodeGladiator</Link></header><section className="mx-auto max-w-5xl py-20"><p className="text-xs font-semibold uppercase tracking-[.25em] text-cyan-200/70">For sponsors</p><h1 className="mt-5 max-w-3xl text-5xl font-semibold tracking-tight sm:text-7xl">Turn a hard problem into your next great hire.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-white/60">Fund a challenge, watch developers prove their ability under pressure, and reward the first verified solution. CodeGladiator makes technical merit visible.</p><div className="mt-12 grid gap-5 md:grid-cols-3">{[['Sponsor-funded', 'Put a real bounty behind a problem your team needs solved.', Building2], ['Merit-based', 'Evaluate practical ability instead of résumé theater.', Sparkles], ['Trust-ready', 'Keep proof and settlement ready for a transparent workflow.', ShieldCheck]].map(([title, text, Icon]) => <div key={title as string} className="liquid-glass rounded-[1.5rem] p-5"><Icon size={20} className="text-cyan-100" /><h2 className="mt-8 font-semibold">{title as string}</h2><p className="mt-2 text-sm leading-6 text-white/50">{text as string}</p></div>)}</div><Link href="/sponsor/fund" className="mt-10 inline-flex min-h-12 items-center gap-2 rounded-full bg-white px-6 text-sm font-semibold text-black">Create a developer bounty <ArrowRight size={16} /></Link></section></main>
}
